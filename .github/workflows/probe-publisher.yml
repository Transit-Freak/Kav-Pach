name: probe-publisher

# בדיקת-כיס לשימוש חוזר: מריצים ידנית קטע בדיקה מול הרשת כשצריך.
# כרגע במצב שקט — לא רץ בדחיפות ולא שולח מיילים.

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "אין בדיקה פעילה כרגע"
