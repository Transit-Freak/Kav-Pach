name: update-weekly

# יומי: רענון שמות התחנות והסיווג מ-GTFS (משרד התחבורה מפרסם קובץ חדש כל יום).
# משתמש ב-POI/כבישים הקיימים ושומר זמני הליכה שכבר חושבו (rt) — בלי בניית OSRM הכבדה.
# (שם הקובץ נשאר update-weekly מסיבות היסטוריות — התזמון בפועל יומי מאז 04.07.2026.)
# (סיווג מעודכן: זיהוי דואר 📮 + הבדל "ה" הידיעה אינו טעות + שם מוצע ממוקד מרכזי עד 100 מ׳
#  + "הצעות כלליות": לתחנות תקינות שבהן הרחוב שבכתובת רחוק והרחוב הקרוב במפה שונה
#    — סינון רעש: התאמת רחובות עמידה למקפים/ר"ת, דילוג על שמות בכתב ערבי/מספריים.)

on:
  schedule:
    - cron: '0 7 * * *'   # כל בוקר 07:00 UTC (10:00 ישראל בקיץ, 09:00 בחורף) — אחרי שעת-הפרסום הנודדת של GTFS במשרד התחבורה
  # (דחיפה: הפעלת סיווג-מחדש אחרי סינון וריאציות כתיב)
  push:
    paths:
      - '.github/workflows/update-weekly.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  # קבוצה נפרדת לכל ref: ריצת ה-push של סנכרון הענף (שמדלגת בגלל תנאי
  # main-בלבד) נכנסת לקבוצה לפני שהתנאי נבדק — בלי זה היא מבטלת את ריצת main
  group: update-weekly-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run:
    if: github.ref == 'refs/heads/main' || github.event_name != 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Fetch fresh GTFS stops + active set
        run: |
          set -euo pipefail
          UA="Mozilla/5.0 (Windows NT 10.0; Win64; x64)"
          URL="https://gtfs.mot.gov.il/gtfsfiles/israel-public-transportation.zip"
          curl -fSL --connect-timeout 20 --max-time 1200 --retry 6 --retry-all-errors --retry-delay 15 -A "$UA" -o gtfs.zip "$URL"
          unzip -o gtfs.zip stops.txt
          unzip -o gtfs.zip stop_times.txt
          unzip -o gtfs.zip translations.txt
          SID=$(head -1 stop_times.txt | tr -d '\r' | tr ',' '\n' | grep -n -x 'stop_id' | cut -d: -f1)
          awk -F, -v c="$SID" 'NR>1{gsub(/\r/,"",$c); if($c!="") a[$c]=1} END{for(k in a) print k}' stop_times.txt | sort > active_stops.txt
          wc -l stops.txt active_stops.txt

      - name: Reuse existing POI + roads from data branches
        run: |
          set -euo pipefail
          git fetch origin osm-poi && git show FETCH_HEAD:poi.json > poi.json
          git fetch origin osm-roads && git show FETCH_HEAD:roads.json > roads.json
          ls -la poi.json roads.json

      - name: Classify (carry forward walking times from current data.json)
        run: |
          set -euo pipefail
          cp next-station/data.json prev.json 2>/dev/null || echo '{}' > prev.json
          STOPS=stops.txt POI=poi.json ROADS=roads.json ACTIVE=active_stops.txt \
            PREV=prev.json HISTORY=next-station/history.json CHANGES=next-station/changes.json CATDIFF=next-station/catdiff.json \
            SNAPSHOT=next-station/stops-names.json \
            OUT=next-station/data.json python3 tools/classify.py

      - name: Detect English-translation errors (טעויות תרגום — קטגוריה באפליקציה)
        run: |
          set -euo pipefail
          python3 -m pip install --quiet pyspellchecker || true
          STOPS=stops.txt TRANS=translations.txt OUTFILE=next-station/translation-errors.json \
            MANUAL=tools/translation-errors-manual.json python3 tools/translation_check.py

      - name: Commit refreshed data.json to main
        run: |
          set -euo pipefail
          rm -f gtfs.zip stops.txt stop_times.txt translations.txt active_stops.txt poi.json roads.json prev.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # main עלול לזוז בזמן הריצה — שומרים פלט, מתעדכנים, מחילים ודוחפים (עם ניסיונות חוזרים)
          cp next-station/data.json /tmp/out-data.json
          cp next-station/history.json /tmp/out-hist.json 2>/dev/null || true
          cp next-station/changes.json /tmp/out-chg.json 2>/dev/null || true
          cp next-station/catdiff.json /tmp/out-catd.json 2>/dev/null || true
          cp next-station/translation-errors.json /tmp/out-trans.json 2>/dev/null || true
          cp next-station/stops-names.json /tmp/out-snap.json 2>/dev/null || true
          for i in 1 2 3 4; do
            git fetch origin main && git reset --hard origin/main
            cp /tmp/out-data.json next-station/data.json
            [ -f /tmp/out-hist.json ] && cp /tmp/out-hist.json next-station/history.json
            [ -f /tmp/out-chg.json ] && cp /tmp/out-chg.json next-station/changes.json
            [ -f /tmp/out-catd.json ] && cp /tmp/out-catd.json next-station/catdiff.json
            [ -f /tmp/out-trans.json ] && cp /tmp/out-trans.json next-station/translation-errors.json
            [ -f /tmp/out-snap.json ] && cp /tmp/out-snap.json next-station/stops-names.json
            git add next-station/data.json next-station/history.json
            git add next-station/changes.json 2>/dev/null || true
            git add next-station/catdiff.json 2>/dev/null || true
            git add next-station/translation-errors.json 2>/dev/null || true
            git add next-station/stops-names.json 2>/dev/null || true
            if git diff --cached --quiet; then
              echo "data unchanged — nothing to commit"; break; fi
            git commit -m "weekly: refresh stop names & classification from GTFS"
            if git push origin HEAD:main; then break; fi
            echo "push rejected (main moved) — retry $i"; sleep 10
          done

# rerun: GTFS fetch timeout was transient (0723Z)

# retry: MOT GTFS endpoint outage (sustained 07:36-07:57Z) — re-run when it recovers
# reclassify: 'הצעות כלליות' now suggests a closer cross-street + emits road geometry for the map

# reclassify: tighten closer cross-rule (named cross must be far >=45m, alt <=30m) to cut noise

# reclassify: stricter closer gap to further cut noise

# reclassify: closer decision now verified by real OSRM walking distance (+rw stored); labels centered on-street

# reclassify: bound OSRM walking with time-budget + circuit-breaker (never hang the job)

# reclassify: walking moved to monthly local-OSRM; weekly carries rw forward (fast, no public OSRM)

# reclassify: drop non-unique "הצעות כלליות" suggestions (same name for multiple stops, e.g. Tifrah)

# reclassify: de-dup suggested names across ALL categories (e.g. רמב"ם ×5 in Tifrah reversal/mismatch)

# reclassify: institution/landmark names (מרפאה, הישיבה/רחוב…) -> ספק, no reversed suggestion; revert error-category de-dup

# reclassify: expand institution/landmark words (שכונה/צומת/מסוף/פארק/חוף/מלון/אזור/קריית/מגדל...) -> ספק; ככר stays spelling

# reclassify: also route מגרש/מכון/מחנה/ת.מרכזית to ספק

# reclassify: עירייה/שוק/רמת/גבעת -> ספק + counts history for trends

# reclassify: source-fix tracking (changes.json) — flagged stop whose GTFS text changed and is no longer flagged = real fix

# reclassify: generic acronym matching (קק"ל = קרן קיימת לישראל) — token letters == word initials

# reclassify: POI-match check now runs before institution->ספק (Big Kastina hidden as landmark); lm flag for tailored UI text

# reclassify: unified — WB+Golan roads/POIs (113,985 roads, 30,326 POIs), 942 cemeteries, OSM date stamps, versioned history

# reclassify: acronym matching now skips the definite article (קק"ל = הקרן הקיימת לישראל)

# reclassify: never suggest non-Hebrew names (Arabic-script roads/POIs) in any category

# reclassify: numbered highways (1,286 roads as כביש N) now in the map data

# reclassify: hyphen ≡ space in name normalization (גיסר א-נסף ↔ גסר א נסף) — 41 false mismatches become exact/spelling

# reclassify: unified — named-only roads (road-90 revert), building POIs (ישיבת הרי יהודה), Latin-name filter

# reclassify: neighborhoods layer landed (1,929 hoods incl. אושיות) + name-bound overrides

# reclassify: numbered highways are back for matching-only (כביש 411 stops -> mapok, never suggested)

# reclassify: care-homes POI layer landed (153 בתי אבות incl. לב אבות)

# reclassify: abbreviated street names (לדזינסקי = עובד לדיזנסקי) are ספק, not טעות כתיב

# reclassify: area POIs (cemetery/park/hood) match up to 800m by centroid; הר -> landmark word (הר הזיתים)

# reclassify: checkpoints layer + Hebrew POI names landed; מעבר/מחסום -> landmark words

# reclassify: suggested names use Israeli stop format X/Y (no spaces around the slash)

# dry-run: count stops whose named cross-street does not exist in the map at all (no behavior change)

# reclassify: enable missing-cross-street rule (well-mapped areas only) + dotted-abbrev fix + אנדרטה/חווה landmarks

# reclassify: missing-cross well-mapped radius 350->500m (moshav stops like ניר וגל/ערבה sit at village edges)

# reclassify: clean namesake matches (identifying-words fix) + pick up מורן if landed

# reclassify: streetvar whose name exactly matches a nearby place (צומת כרם) -> landmark, not variance

# reclassify: new category שם מטעה — namesake unreachable by foot (2x aerial / no route)

# reclassify: מסוף/תחנה מרכזית stops are the terminal itself — never an error, hidden entirely

# reclassify: operational stops (תחנה תפעולית) are service stops — hidden like terminals

# reclassify: מסחרי/מחנה/סופר are generic — מרכז מסחרי and מחנה עופרית leave שם מטעה

# reclassify: remote namesake matching is exact-token only (אביק != אביב); מפעל is generic

# reclassify: generic words blocked with definite article too (הכנסת = כנסת)

# reclassify: דלק/כללית/אבן are generic — last namesake noise family

# reclassify: remove the שם מטעה category entirely — too many false positives

# reclassify: joined-words comparison (קוסטריקה = קוסטה ריקה) — one-word spellings match their street

# reclassify: joined-word with a letter typo (קוסטריקה) is a spelling error, not ספק

# rerun: resume category comparison (first run after rules stabilized)

# reclassify: catdiff.json — which stops changed category, opened from the trend arrows
